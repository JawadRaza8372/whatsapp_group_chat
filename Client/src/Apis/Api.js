const server="http://localhost:5000";
const postmessage=`${server}/messages/new`;
const fetchmessage=`${server}/messages/sync`;
const newroom=`${server}/room/new`;
const fetchroom=`${server}/room/sync`;
export {postmessage,fetchmessage,newroom,fetchroom}

